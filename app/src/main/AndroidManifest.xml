<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Licentatest">
        <activity
            android:name=".activities.AIDiagnosticActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddCarActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MyGarageActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PartsListActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CarPartsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.WarningLightsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="true" />
        <activity
            android:name=".activities.HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" />
    </application>

</manifest>